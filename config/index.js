const { firebaseConfig, serviceAccount, databaseURL } = require('./firebase')
const { adafruitKey, adafruitUsername } = require('./adafruit')

module.exports = {
    firebaseConfig,
    serviceAccount,
    databaseURL,
    adafruitKey,
    adafruitUsername,
}